%form{:method => "POST", :action => '/comment'}
  %input{:type => "hidden", :name => "_method", :value => "PUT"}
  %table
    %tr
      %td 名前
      %td 
        %input{:type => "text", :name => "name"}
    %tr
      %td タイトル
      %td
        %input{:type => "text", :name => "title"}
    %tr
      %td 内容
      %td
        %textarea{:name => "message", :cols => 60, :rows => 8}
    %tr
      %td
      %td
        %input{:type => "submit"}

- @comments.each do |comment|
  .comment
    %h2== #{h comment.id} #{(comment.title != "") ? (h comment.title) : 'タイトル無し'} 
    .info
      %span.name== by #{(comment.name.empty?) ? '名無しさん' : (h comment.name)}
      %span.date== (#{h comment.date})
      %a{:href => "/delete?id=#{comment.id}"} コメントを削除

    .message
      = comment.formatted_message
